pipeline {
    agent any
    stages {
        stage ("code") {
            steps {
                echo "this for code"
            }
        }
        stage ("build") {
            steps {
                echo "this is for code build"
            }
        }
    }
}
